import 'package:flutter/material.dart';

class AppColors {
  static const Color blackColor = Color.fromRGBO(255, 0, 255, 0);
  static const Color whiteColor = Colors.white;
  static const Color buttonColor = Colors.green;
}
